def bool_to_binary(bol):
    """
    Receives a boolean and returns '1' if it's true and '0' if it's false
    """
    if bol == True:
        return 1
    return 0


print(bool_to_binary(True))
print(bool_to_binary(False))
